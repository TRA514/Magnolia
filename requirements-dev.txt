# Install: python3 -m pip install --break-system-packages -r requirements-dev.txt
# (Homebrew python3 is PEP-668 externally-managed; cross-platform install is handled by the Doctor layer in Phase 2.)
pytest>=8.0
ruamel.yaml>=0.18
